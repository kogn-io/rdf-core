# Maven
target/
*.class

# Eclipse
.settings/
.project
.classpath
.factorypath
.apt_generated/
bin/
*.eclipse

# IntelliJ
.idea/
*.iml

# VS Code
.vscode/

# Claude Code — local scratch, in particular the git worktrees it checks out
# inside the repo. These are separate repositories; committing one lands as a
# gitlink that no clone can resolve.
.claude/worktrees/
