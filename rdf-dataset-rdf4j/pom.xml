<project
  xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd"
>
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.kogn.rdf</groupId>
    <artifactId>rdf-parent</artifactId>
    <version>${revision}</version>
    <relativePath>../pom.xml</relativePath>
  </parent>
  <artifactId>rdf-dataset-rdf4j</artifactId>
  <name>Kogn RDF Dataset RDF4J Implementation</name>
  <description
  >RDF4J-based implementation of the rdf-dataset ports (GraphStore, SparqlQuery, SparqlUpdate, DatasetExport, DatasetTransactor/DatasetTx) plus RDF4J term/factory helpers. Store-agnostic wrappers over a caller-supplied Repository; dataset hosting lives in rdf-dataset-hosting-rdf4j.</description>

  <dependencies>
    <!-- Own RDF modules: terms + dataset ports only; zero api/cid -->
    <dependency>
      <groupId>io.kogn.rdf</groupId>
      <artifactId>rdf-terms</artifactId>
    </dependency>
    <dependency>
      <groupId>io.kogn.rdf</groupId>
      <artifactId>rdf-dataset</artifactId>
    </dependency>

    <!-- RDF4J: only what the moved classes actually import -->
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-model</artifactId>
    </dependency>
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-model-api</artifactId>
    </dependency>
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-repository-api</artifactId>
    </dependency>
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-query</artifactId>
    </dependency>
    <!-- SailConflictException, mapped to the neutral ConcurrencyConflictException by DatasetTransactorRdf4j -->
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-sail-api</artifactId>
    </dependency>
    <!-- Rio: RDFFormat/Rio/RDFHandlerException, used by DatasetExportRdf4j -->
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-rio-api</artifactId>
    </dependency>

    <!-- The four writers DatasetExport promises, resolved by Rio through its ServiceLoader
         registry: needed at runtime, never imported, so runtime scope. Unlike the concrete
         Sail stores below these are not a consumer choice — the port names exactly these
         formats, so the adapter has to be able to write them. (trig pulls in turtle and
         nquads pulls in ntriples, but each is declared: what is promised is listed.) -->
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-rio-turtle</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-rio-ntriples</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-rio-trig</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-rio-nquads</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- Lombok (annotation processor, not a runtime dep) -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- Concrete Sail stores are needed only by DatasetRdf4jTest, which builds a
         Repository to exercise the wrappers against; the wrappers themselves are
         store-agnostic. Store construction proper lives in rdf-dataset-hosting-rdf4j. -->
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-repository-sail</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-sail-memory</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-sail-nativerdf</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
